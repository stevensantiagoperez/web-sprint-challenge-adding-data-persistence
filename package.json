{
  "name": "adding-data-persistence",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "server": "nodemon index.js",
    "migrate": "knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "reset": "npm run rollback && npm run migrate && np run seed",
    "seed": "knex seed:run",
    "test": "cross-env NODE_ENV=testing jest --verbose --runInBand --silent"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BloomInstituteOfTechnology/web-sprint-challenge-adding-data-persistence.git"
  },
  "license": "ISC",
  "devDependencies": {
    "cross-env": "7.0.3",
    "eslint": "8.14.0",
    "jest": "28.1.0",
    "knex-cleaner": "^1.3.1",
    "nodemon": "^3.1.10",
    "supertest": "6.2.3"
  },
  "dependencies": {
    "sqlite3": "^5.0.8"
  }
}
